{
    "require-dev": {
        "phpdocumentor/phpdocumentor": "^3.1"
    },
    "config": {
        "allow-plugins": {
            "symfony/flex": false
        }
    },
    "scripts": {
        "docs": [
            "phpdoc -d controllers -d core -d models -t docs --template=clean"
        ]
    }
            
}
